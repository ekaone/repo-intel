# repo-intel-darwin-x64

Platform binary for [macOS x64 (Intel)] — part of [repo-intel](https://www.npmjs.com/package/repo-intel).

**Do not install this package directly.** It is an optional dependency automatically selected by `repo-intel` based on your OS and CPU architecture.

## Install via the main package

```bash
npm install -g repo-intel
```