# repo-intel-linux-x64

Platform binary for [Linux x64] — part of [@ekaone/repo-intel](https://www.npmjs.com/package/@ekaone/repo-intel).

**Do not install this package directly.** It is an optional dependency automatically selected by `@ekaone/repo-intel` based on your OS and CPU architecture.

## Install via the main package

```bash
pnpm add -g @ekaone/repo-intel @ekaone/repo-intel-linux-x64
```